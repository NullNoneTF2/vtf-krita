--- vtflib-pristine/CMakeLists.txt	2026-07-12 15:22:00.428823637 +0000
+++ vtf_full_source/VTFLib/CMakeLists.txt	2026-07-12 15:21:28.784362956 +0000
@@ -54,7 +54,19 @@
 		set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} /W4")
 	endif()
 elseif(CMAKE_COMPILER_IS_GNUCC OR CMAKE_COMPILER_IS_GNUCXX)
-	set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11 -Wall -Wextra -pedantic -Werror -O3")
+	set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11 -Wall -Wextra -pedantic -Wno-error -Wno-attributes -O3")
 endif()
 
+if((CMAKE_COMPILER_IS_GNUCC OR CMAKE_COMPILER_IS_GNUCXX) AND WIN32)
+	# Statically link the MinGW runtime (libgcc / libstdc++ / libwinpthread) into
+	# libVTFLib13.dll. Without this, the DLL only loads on a machine that happens
+	# to have a matching MSYS2/MinGW install (like the CI runner) providing
+	# libgcc_s_seh-1.dll, libstdc++-6.dll and libwinpthread-1.dll on PATH. End
+	# users installing the plugin into Krita normally have none of these, which
+	# surfaces as a confusing "Could not find module ... (or one of its
+	# dependencies)" error from ctypes even though the DLL file itself is present.
+	set(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} -static-libgcc -static-libstdc++ -Wl,-Bstatic,--whole-archive -lwinpthread -Wl,--no-whole-archive")
+	set(CMAKE_SHARED_LINKER_FLAGS "${CMAKE_SHARED_LINKER_FLAGS} -static-libgcc -static-libstdc++ -Wl,-Bstatic,--whole-archive -lwinpthread -Wl,--no-whole-archive")
+endif()
+
 if(USE_NVDXT AND ${CMAKE_SYSTEM_NAME} STREQUAL "Windows")
--- vtflib-pristine/src/VTFFile.cpp	2026-07-12 15:22:00.432823637 +0000
+++ vtf_full_source/VTFLib/src/VTFFile.cpp	2026-07-12 15:21:28.878593844 +0000
@@ -71,6 +71,22 @@
 
 #	include <GL/gl.h>
 
+// Some GL/gl.h headers (notably MinGW's) don't pull in the S3TC extension
+// tokens, so define them ourselves if missing. Values match the official
+// GL_EXT_texture_compression_s3tc registry entry.
+#	ifndef GL_COMPRESSED_RGB_S3TC_DXT1_EXT
+#		define GL_COMPRESSED_RGB_S3TC_DXT1_EXT 0x83F0
+#	endif
+#	ifndef GL_COMPRESSED_RGBA_S3TC_DXT1_EXT
+#		define GL_COMPRESSED_RGBA_S3TC_DXT1_EXT 0x83F1
+#	endif
+#	ifndef GL_COMPRESSED_RGBA_S3TC_DXT3_EXT
+#		define GL_COMPRESSED_RGBA_S3TC_DXT3_EXT 0x83F2
+#	endif
+#	ifndef GL_COMPRESSED_RGBA_S3TC_DXT5_EXT
+#		define GL_COMPRESSED_RGBA_S3TC_DXT5_EXT 0x83F3
+#	endif
+
 // txc_dxtn.h is not included in the libtxc_dxtn package of common Linux distributions
 // and there is no libtxc_dxtn-devel package (on Fedora), but I only need one function anyway.
 // See: http://cgit.freedesktop.org/~mareko/libtxc_dxtn/
@@ -2954,13 +2970,13 @@
 	case IMAGE_FORMAT_DXT1:
 	case IMAGE_FORMAT_DXT3:
 	case IMAGE_FORMAT_DXT5:
-		if (uiWidth % 4 != 0)
+		if (uiWidth >= 4 && uiWidth % 4 != 0)
 		{
 			LastError.SetFormatted("Width for DXT compressed texture is not a multiple of 4: %u", uiWidth);
 			throw 0;
 		}
 
-		if (uiHeight % 4 != 0)
+		if (uiHeight >= 4 && uiHeight % 4 != 0)
 		{
 			LastError.SetFormatted("Height for DXT compressed texture is not a multiple of 4: %u", uiHeight);
 			throw 0;